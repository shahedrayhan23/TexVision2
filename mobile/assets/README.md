Place your app icon (icon.png, 1024x1024) and splash assets here.
For the hackathon demo, Expo will use default placeholders if these are missing —
the app will still run fine in Expo Go without custom assets.
